<h1>Movie</h1>

<%= flash[:notice] %>
<table>
	<thead>
		<th>Title</th>
		<th>description</th>
	</thead>
	<tbody>
		<% @movies.each do |movie| %>
			<tr>
				<td><%= movie.title %></td>
        <td><%= movie.description %></td>
			</tr>
		<% end %>
	</tbody>
</table>
